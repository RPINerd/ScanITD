[tox]
envlist = py310, py311, py312, py313, py314, lint

[testenv:lint]
basepython = python
deps = ruff
commands = ruff check src tests

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    pytest
    pytest-cov
commands = pytest
